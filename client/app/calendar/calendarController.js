var calendar = angular.module('calendar', []);
calendar.controller('calendarController', ['auth', '$window', 'Events', function(auth, $window, Events) {
	var that = this;
	that.eventSources = [];


}]);